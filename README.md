# Lista3_MatheusClemente

Programa gera um vetor de tamanho e valor máximo definidos pelo usuário. Após ordená-lo pelos métodos shell, quick e bucket sort, retorna o tempo de execução de cada algoritmo.
